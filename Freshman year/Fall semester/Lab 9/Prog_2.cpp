/***    Full name: MEKHROL BAZAROV
		Student ID: U2010046
		Program statement: LAB-9. 2. Write a program to find average of array elements ***/
	/*
#include<iostream> // Header File
using namespace std; // Pre processor
int main2() // change here to int main() if there is a number main2
{
	int a[46]; // declared array   // 46 final number of my ID
	int m, sum=0;
	float average;
	cout << "Input number of elements in an array:" << endl; //MUST BE POSITIVE NUMBER
	cin >> m;
	cout << "Input array elements: " << endl;
	for (int i = 0; i < m; i++)
	{
		cin >> a[i];  //Input array elements
	}

	cout << "---------------------" << endl;
	cout << "Array elements are = " << endl;

	for (int i = 0; i < m; i++)
	{
		cout << "a[" << i << "] " << "= " << a[i] << endl;  // print elemnt that we inputed
		sum += a[i]; // elements added every step 	
	}

	cout << "Sum of array elements = " << sum << endl; // we get sum of array elements
	average = sum / m; // formula to find average of array elements
    cout << "Average of array elements = " << average << endl; // average is printed 
	system("pause");
	return 0;
}*/
