/***    Full name: MEKHROL BAZAROV
		Student ID: U2010046
		Program statement: LAB-9. 5. Write a program to delete an element from an array ***/
		
		// there was taught in the ppt file and lecture video 4 week 12
/* 
#include<iostream> // Header File
using namespace std; // Pre processor
int main5() // change here to int main() if there is a number main5
{
	int n, pos, num;
	int a[10];
	cout << "Input number of elements in an array:" << endl;
	cin >> n;
	cout << "Input array elements: Enter positive numbers: " << endl;
	for (int i = 0; i < n; i++)
	{
		cin >> a[i];  // inputted 
	}
	cout << "---------------------" << endl;
	cout << "Array elements are" << endl;

	for (int i = 0; i < n; i++)
	{
		cout << "a [" << i << "] = " << a[i] << endl;  // output
	}
	cout << endl;

	cout << "Enter position you want to delete from an array = " << endl; // you can also write 0 because it will start at 0 position 
	cin >> pos;

	for (int i = pos; i < n; i++)
	{
		a[i] = a[i + 1];                        //change position
	}
	n--;

	for (int i = 0; i < n; i++)
	{
		cout << "a [" << i << "] = " << a[i] << endl;   // print
	}
	cout << endl;

	system("pause");
	return 0;
}*/
